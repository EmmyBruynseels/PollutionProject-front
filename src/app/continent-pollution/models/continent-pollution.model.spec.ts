import { ContinentPollution } from './continent-pollution.model';

describe('ContinentPollution', () => {
  it('should create an instance', () => {
    expect(new ContinentPollution()).toBeTruthy();
  });
});
